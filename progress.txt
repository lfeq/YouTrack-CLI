2026-06-03: Completed Issue #01 (Project scaffold + `youtrack login`).
- Created `pyproject.toml` with package entry points and dependencies.
- Implemented `config` module in `src/youtrack_cli/config.py` for reading/writing configuration (supporting `YOUTRACK_TOKEN` and `YOUTRACK_CONFIG_DIR` environment variables).
- Implemented `cli` skeleton and `youtrack login` command in `src/youtrack_cli/cli.py`.
- Wrote unit/integration tests in `tests/test_config.py` and `tests/test_cli.py`. All tests pass.
- Moved issue `01-project-scaffold-and-login.md` to `issues/completed/`.

2026-06-03: Completed Issue #02 (youtrack project list + youtrack project create).
- Implemented `client` module in `src/youtrack_cli/client.py` wrapping `httpx`, handling Bearer token authorization headers, and converting HTTP errors to typed `YouTrackAPIError` exceptions.
- Implemented `projects` module in `src/youtrack_cli/projects.py` with support for listing projects and creating projects (by fetching leader ID via `GET /api/users/me` first).
- Implemented `formatters` module in `src/youtrack_cli/formatters.py` to support formatting project lists as aligned text tables or JSON arrays.
- Wired subcommands `project list` and `project create` into `cli.py`.
- Wrote unit and integration tests for `client`, `projects`, and CLI commands in `tests/test_client.py`, `tests/test_projects.py`, and `tests/test_cli.py`. All tests pass.
- Moved issue `02-project-list-and-create.md` to `issues/completed/`.

2026-06-03: Completed Issue #03 (`youtrack issue create`).
- Created `issues` module in `src/youtrack_cli/issues.py` with `Issue` domain class and `create_issue` function.
- Implemented logic to resolve the project's internal `id` by listing projects first (using `api/admin/projects?fields=id,shortName`) and performing a case-insensitive search by project shortName.
- Handled optional custom fields (`Assignee` as `SingleUserIssueCustomField`, `Priority` and `Type` as `SingleEnumIssueCustomField`) to map to YouTrack's expected API payload.
- Added `format_issue_json` in `src/youtrack_cli/formatters.py` to format single issues.
- Wired subcommand `issue create` into `cli.py` with support for optional arguments and `--json` format.
- Wrote unit and integration tests for `issues` and CLI commands in `tests/test_issues.py` and `tests/test_cli.py`. All tests pass.
- Moved issue `03-issue-create.md` to `issues/completed/`.

2026-06-03: Completed Issue #04 (youtrack issue list with filtering).
- Extended the `Issue` dataclass with `status` and `assignee` optional fields (defaulting to None for compatibility with existing tests).
- Implemented `list_issues` in `src/youtrack_cli/issues.py` to compile specific filters (`--tag`, `--status`, `--assignee`) along with raw `--query` into a combined YouTrack query string.
- Handled API response parsing to extract `State` (as `status`) and `Assignee` values from custom field arrays.
- Implemented `format_issues_table` and `format_issues_json` in `src/youtrack_cli/formatters.py` to support CLI table and JSON display formats.
- Wired `youtrack issue list` CLI subcommand into `cli.py` with all specific filters and JSON options.
- Wrote unit and integration tests for query merging, parsing logic, CLI flag binding, and help menus in tests/test_issues.py and tests/test_cli.py. All tests pass.
- Moved issue `04-issue-list-with-filtering.md` to `issues/completed/`.

2026-06-03: Completed Issue #05 (youtrack issue move).
- Implemented `move_issue` in `src/youtrack_cli/issues.py` to update an issue's status (State) via YouTrack's `POST /api/issues/{issue_id}` endpoint.
- Wired the `youtrack issue move` CLI command in `src/youtrack_cli/cli.py` with the positional `issue_id` argument and the `--status` option.
- Wrote TDD tests: unit tests in `tests/test_issues.py` and CLI integration tests in `tests/test_cli.py` to cover successful moves, API error handling, and argument/option validation. All tests pass.
- Moved issue `05-issue-move.md` to `issues/completed/`.

2026-06-03: Completed Issue #06 (youtrack tag create + youtrack issue tag).
- Created the `tags` module in `src/youtrack_cli/tags.py` with `Tag` domain class and `create_tag` function.
- Implemented `tag_issue` in `src/youtrack_cli/issues.py` to fetch existing tags (using `GET /api/tags?fields=id,name`), match the requested tag by name, and apply it to an issue (via `POST /api/issues/{issue_id}/tags?fields=id,name`).
- Added subcommand `tag create` under `tag` group, and subcommand `tag` under `issue` group.
- Wrote unit and CLI integration tests for tag creation, tag assignment, tag existence validation, and CLI command help blocks. All tests pass.
- Moved issue `06-tag-create-and-issue-tag.md` to `issues/completed/`.

2026-06-03: Completed Issue #10 (Migrate build tooling to uv).
- Migrated `pyproject.toml` to use `uv_build` as the PEP 517 build backend and removed setuptools configuration.
- Configured PEP 735 `[dependency-groups]` for development dependencies (`pytest` and `respx`) to ensure automatic sync.
- Created `.gitignore` to exclude `.venv/`, `build/`, `dist/`, caches, and egg-info folders.
- Ran `uv sync` to lock dependencies and generate `uv.lock`.
- Verified the build and environment via `uv run pytest`, `uv run youtrack --help`, and `uv tool install .`.
- Removed legacy setuptools artifacts (`build/` and `src/*.egg-info/`).
- Moved YouTrack issue `YTCLI-10` to "Fixed" state.

2026-06-03: Completed Issue #13 (Add --tag flag to issue create for atomic tag assignment).
- Extended the `create_issue` function in `src/youtrack_cli/issues.py` with an optional `tags` parameter.
- Implemented in-memory tag resolution from a single `GET api/tags?fields=id,name` API call to fetch all user-visible tags.
- Added fail-fast validation in `create_issue` that raises `YouTrackAPIError` if any requested tag does not exist.
- Wired the `--tag` repeatable option into the `youtrack issue create` command in `src/youtrack_cli/cli.py`.
- Wrote TDD tests covering single tag resolution, multiple tags resolution, and fail-fast validation in `tests/test_issues.py` and `tests/test_cli.py`. All tests pass.
- Moved YouTrack issue `YTCLI-13` to "Fixed" state.

2026-06-03: Completed Issue #12 (Add youtrack tag list command).
- Implemented the `list_tags` function in `src/youtrack_cli/tags.py` to fetch all tags visible to the authenticated user.
- Added `format_tags_table` and `format_tags_json` formatting functions in `src/youtrack_cli/formatters.py` to output tags in a human-readable table or as a JSON array.
- Registered the `youtrack tag list` command in `src/youtrack_cli/cli.py` with the `--json` option.
- Wrote TDD tests for tag list fetching, formatting (for empty and non-empty lists), and CLI invocation. All tests pass.
- Moved YouTrack issue `YTCLI-12` to "Fixed" state.

2026-06-03: Completed Issue #14 (Add youtrack issue types command to list valid type values per project).
- Implemented `list_issue_types` in `src/youtrack_cli/issues.py` to resolve the project's internal ID and retrieve custom fields, filtering for the "Type" custom field and returning its bundle value names.
- Added `format_issue_types_table` and `format_issue_types_json` formatting functions in `src/youtrack_cli/formatters.py` to format issue types as a human-readable table or a JSON array of strings.
- Registered the `youtrack issue types` CLI command under the `issue` command group in `src/youtrack_cli/cli.py` with `--project` and `--json` options.
- Wrote TDD tests covering project ID resolution, custom field fetching, error handling for unknown projects, formatting variations (empty/non-empty), and Click CLI options. All tests pass.
- Moved YouTrack issue `YTCLI-14` to "Fixed" state.
- Moved the completed PRD file to `docs/prd/completed/tag-management-and-issue-types-PRD.md`.
2026-06-03: Completed Issue #11 (Write README and add MIT license).
- Created `LICENSE` file with MIT license text.
- Created `README.md` with project description, installation options, quickstart instructions, configuration guidelines, full command reference, and contributing guidelines.
- Updated `pyproject.toml` to reference `README.md` and the `LICENSE` file.
- Moved YouTrack issue `YTCLI-11` to "Fixed" state.

2026-06-03: Completed Issue #15 (Test tagging issue creation).
- Verified the issue YTCLI-15 "Test tagging issue creation" created by Ixubot.
- Confirmed all unit and CLI integration tests pass.
- Moved YouTrack issue YTCLI-15 to "Fixed" state.

2026-06-03: Completed Issue #16 (Add issue link command and link_subtask core).
- Implemented `link_subtask` in `src/youtrack_cli/issues.py` to link issues as parent/subtask using YouTrack's `/api/commands` endpoint.
- Wired the `youtrack issue link` CLI command in `src/youtrack_cli/cli.py` to expose this capability.
- Wrote unit and integration tests in `tests/test_issues.py` and `tests/test_cli.py` to cover successful linking, API error propagation, and Click options/arguments validation. All tests pass.
- Moved YouTrack issue `YTCLI-16` to "Fixed" state.

2026-06-03: Completed Issue #20 (Add --parent flag to issue create).
- Extended the `create_issue` function in `src/youtrack_cli/issues.py` with an optional `parent` parameter.
- Implemented logic in `create_issue` to call `link_subtask` if `parent` is provided, linking the newly created child issue under the parent Epic.
- Added the `--parent` option to the `youtrack issue create` Click command in `src/youtrack_cli/cli.py` and passed it down to `create_issue`.
- Wrote TDD unit and CLI integration tests in `tests/test_issues.py` and `tests/test_cli.py` to cover parent linking on success and API/linking failure. All tests pass.
- Moved YouTrack issue `YTCLI-20` to "Fixed" state.

2026-06-03: Completed Issue #17 (Add issue comment command for append-only progress notes).
- Introduced the `Comment` dataclass to model comment attributes (id, text, author, created).
- Implemented `add_comment` in `src/youtrack_cli/issues.py` to make a POST request to YouTrack's comment API, mapping nested `author` to a string representation.
- Wired the `youtrack issue comment` CLI subcommand in `src/youtrack_cli/cli.py`.
- Wrote TDD unit tests in `tests/test_issues.py` and integration tests in `tests/test_cli.py`. All tests pass.
- Moved YouTrack issue `YTCLI-17` to "Fixed" state.

2026-06-03: Completed Issue #21 (Add issue show command for detailed view of an issue with comments).
- Introduced the `IssueDetail` dataclass carrying the issue fields plus a list of `Comment` objects.
- Implemented `show_issue` function in `src/youtrack_cli/issues.py` to retrieve an issue and its comments in a single REST API request.
- Implemented formatting functions `format_issue_detail_table` and `format_issue_detail_json` in `src/youtrack_cli/formatters.py`.
- Registered `youtrack issue show` command in `src/youtrack_cli/cli.py` supporting both standard text output and `--json` formatting.
- Wrote TDD unit tests in `tests/test_issues.py` and CLI integration tests in `tests/test_cli.py` covering successful retrieves (with and without comments), error propagation, and output formatting. All tests pass.
- Moved YouTrack issue `YTCLI-21` to "Fixed" state.

2026-06-03: Completed Issue #19 (Add --unresolved and --top filters to issue list).
- Extended the `list_issues` function in `src/youtrack_cli/issues.py` to support `unresolved: bool` and `top: Optional[int]` parameters.
- Implemented query assembly logic in `list_issues` to append `"#Unresolved"` to the YouTrack query string if `unresolved` is True.
- Implemented pagination capping logic in `list_issues` to pass the `"$top"` parameter in the REST API request if `top` is greater than 0 (defaulting to 50), and omitting it if `top` is 0 or None.
- Registered the `--unresolved` flag and `--top` (default 50) option under the `youtrack issue list` CLI command in `src/youtrack_cli/cli.py`.
- Wrote TDD unit tests in `tests/test_issues.py` and CLI integration tests in `tests/test_cli.py` covering unresolved query merging, top-capping parameter inclusion/omission, Click option parsing, and CLI help menu updates. All tests pass.
- Verified behavior against the live YouTrack instance.
- Moved YouTrack issue `YTCLI-19` to "Fixed" state.

2026-06-03: Completed Issue #18 (Add issue update command for description/summary full replacement).
- Implemented `update_issue` in `src/youtrack_cli/issues.py` to full-replace an issue's description and/or summary via a POST request to `api/issues/{issue_id}`.
- Handled early validation in `update_issue` to raise a `ValueError` if neither parameter is provided, making no API call.
- Registered the `youtrack issue update` Click subcommand in `src/youtrack_cli/cli.py` with `--description` and `--summary` options, and raising a Click `UsageError` if both options are omitted.
- Wrote TDD unit tests in `tests/test_issues.py` and CLI integration tests in `tests/test_cli.py` to assert correct requests, input validations, formatting/error handling, and Click configurations. All tests pass.
- Moved YouTrack issue `YTCLI-18` to "Fixed" state.

2026-06-03: Completed Issue #22 (Wire to-prd and to-issues skills to the PRD Epic workflow).
- Updated `.claude/skills/to-prd/SKILL.md` to instruct the agent to publish the PRD as an issue of type `Epic` using the `youtrack issue create` command with `--type Epic` and `--description "<prd>"`, passing the `ready-for-agent` tag and emitting the new Epic ID.
- Updated `.claude/skills/to-issues/SKILL.md` to accept the parent PRD Epic ID, fetch the PRD content with `youtrack issue show`, and pass `--parent <epic-id>` to the `youtrack issue create` command for all implementation issues.
- Updated the local PRD `docs/prd/prd-publishing-and-agent-workflow-PRD.md` to mark all requirements/user stories as completed and moved it to `docs/prd/completed/`.
- Moved YouTrack issue `YTCLI-22` to the "Fixed" state.
- Note for the next person: The PRD is fully complete, and all issues are marked as completed. There are no remaining unresolved issues in YouTrack.

2026-06-03: Completed Issue #26/#27 (Priority discovery command: issue priorities).
- Verified and confirmed the full implementation of `list_issue_priorities` in `src/youtrack_cli/issues.py` (same two-step pattern as `list_issue_types`: project ID resolution then custom fields fetch filtering for "Priority").
- Verified and confirmed `format_issue_priorities_table` and `format_issue_priorities_json` formatters in `src/youtrack_cli/formatters.py`.
- Verified and confirmed `youtrack issue priorities --project <ID> [--json]` CLI command registered under the `issue` group in `src/youtrack_cli/cli.py`.
- Updated `CONTEXT.md`: added `priorities` to the `issue` verb table; updated `--project` flag scope to include `issue priorities`.
- All 117 tests pass. Live smoke test against YTCLI project returns: Show-stopper, Critical, Major, Normal, Minor.
- Moved YouTrack issues `YTCLI-26` and `YTCLI-27` to "Fixed" state.
- Note for the next person: Remaining open issues are YTCLI-28 (--version flag) and YTCLI-29 (README upgrade instructions) — both are small, standalone improvements.

2026-06-03: Completed Issue #28 (Add --version flag to youtrack CLI).
- Added `from importlib.metadata import version as _pkg_version` import to `src/youtrack_cli/cli.py`.
- Decorated the `main` click Group with `@click.version_option(version=_pkg_version("youtrack-cli"), prog_name="youtrack")`.
- Wrote TDD test `test_cli_version` in `tests/test_cli.py` asserting exit_code==0 and "0.2.0" in output.
- Version is read dynamically from installed package metadata so it stays in sync with pyproject.toml automatically.
- Note for the next person: Only YTCLI-29 (README upgrade instructions) remains open — it is purely documentation.

2026-06-03: Completed Issue #29 (Add upgrade instructions to README).
- Added "Updating" section to README.md immediately after "Installation" and before "Quickstart".
- Section shows `pipx upgrade youtrack-cli` and `uv tool upgrade youtrack-cli` commands matching the style of the Installation section.
- No other README content was changed per the issue's acceptance criteria.
- Note for the next person: All issues are now resolved. The prd-publishing-and-agent-workflow PRD is fully complete. The README command reference table is outdated (missing link/comment/show/update/priorities commands) — a follow-up issue could be filed for that if needed.

2026-06-04: Completed Issue YTCLI-31 (Show Priority in issue list output).
- Added `priority` field to the `Issue` dataclass.
- Updated `list_issues` and `move_issue` to parse the `Priority` custom field from the REST API responses.
- Reordered and updated the human-readable `issue list` output format in `format_issues_table` to include a `PRIORITY` column between `ID` and `STATUS` (`ID | PRIORITY | STATUS | SUMMARY | ASSIGNEE`).
- Ensured that `priority` is automatically included in JSON responses when `--json` is requested.
- Wrote TDD tests in `tests/test_issues.py` and `tests/test_cli.py` to cover priority parsing, table formatting, and JSON serialisation. All 119 tests pass.
- Moved YouTrack issue `YTCLI-31` to "Fixed" state.
- Note for the next person: Issue YTCLI-31 is complete. The next feature is YTCLI-32 (Pending-by-default issue listing with priority ordering), which implements the core query generation rules and default filter/sort behavior.

2026-06-04: Completed Issue YTCLI-32 (Pending-by-default issue listing with priority ordering).
- Created pure function `build_issue_list_query()` in `src/youtrack_cli/issues.py` to centralize all YouTrack search query generation logic.
- Updated `list_issues()` to call `build_issue_list_query()` and accept the `all: bool` parameter.
- Added `--all` option to `youtrack issue list` click CLI command in `src/youtrack_cli/cli.py` to allow including resolved issues in listing results.
- Retained `--unresolved` option as a harmless no-op for backward compatibility.
- Updated query builder to suppress default `#Unresolved` filter when `--all`, status, or a raw query is provided, and default sorting when the raw query has custom sorting.
- Wrote 7 unit tests covering default, --all, status, raw query, and parameter composition in `tests/test_issues.py` and updated/added integration tests in `tests/test_cli.py`. All 128 tests pass.
- Moved YouTrack issue `YTCLI-32` to "Fixed" state.
- Note for the next person: Issue YTCLI-32 is complete. The next feature is YTCLI-33 (Update README with full command reference) to document all the commands, flags, and new issue listing behavior in the README.

2026-06-04: Completed Issue YTCLI-33 (Update README with full command reference).
- Added global options (`--version`, `--help`) to the README.
- Documented all noun/verb commands: `login`, `project create|list`, `issue create|list|show|move|tag|link|comment|update|types|priorities`, `tag create|list` with their exact descriptions, options, parameters, and examples.
- Documented `issue list` smart defaults: pending-only by default (using `#Unresolved`), priority-ordering (`priority asc, State asc`), server-side sorting, default suppression behaviors (`--all`, `--status`, raw `--query`), and column/json structure.
- Documented the repeatable `--tag` option and `--priority`/`--parent` flags on `issue create`.
- Confirmed vocabulary matches glossaries in CONTEXT.md (e.g. Issue, Project, Status, Priority, Pending, Tag, Epic, Subtask link, Comment).
- Moved YouTrack issue `YTCLI-33` to "Fixed" state.
- Note for the next person: Issue YTCLI-33 is complete. The next and final task in the PRD Epic is YTCLI-34 (Bump version to 1.0.0). Since this is the last task, the PRD itself is now ready to be marked complete once YTCLI-34 is finished.

2026-06-04: Completed Issue YTCLI-34 (Bump version to 1.0.0).
- Bumped version to 1.0.0 in `pyproject.toml` and updated `uv.lock` via `uv sync`.
- Verified that all 128 unit/integration tests pass.
- Moved the completed PRD file to `docs/prd/completed/priority-ordered-issue-listing-PRD.md`.
- Moved YouTrack issue `YTCLI-34` and the parent Epic `YTCLI-30` to "Fixed" state.
- Note for the next person: The priority-ordered issue listing Epic/PRD is fully complete. The next feature to implement is YTCLI-35 (Native dependency links between issues), using the drafted `docs/prd/native-dependency-links-PRD.md` and `docs/adr/0005-native-dependency-links.md`.

2026-06-04: Completed Issue YTCLI-35 (Native dependency links between issues).
- Implemented `add_dependency` and `remove_dependency` functions in `src/youtrack_cli/issues.py` sending `depends on` / `remove depends on` commands via POST `api/commands`.
- Implemented `parse_dependency_links` pure function in `src/youtrack_cli/issues.py` to parse raw `links` field from YouTrack REST API, extracting `depends_on`, `required_for`, and derived `blocked` status.
- Wired `youtrack issue depend <issue_id> --on <target_id> [--remove]` Click CLI command in `src/youtrack_cli/cli.py`.
- Updated `list_issues` to fetch and parse the `links` field, setting `blocked` on the `Issue` dataclass.
- Updated `format_issues_table` in `src/youtrack_cli/formatters.py` to prepend `[BLOCKED]` to summary of blocked issues.
- Updated `show_issue` to request `links` field and populate `depends_on` and `required_for` fields on `IssueDetail` dataclass.
- Updated `format_issue_detail_table` to render `Depends on:` and `Required for:` sections, highlighting blocking unresolved dependencies as `(BLOCKING)`.
- Updated `tests/test_issues.py` and `tests/test_cli.py` with comprehensive unit and CLI integration tests for dependencies. All 141 tests pass.
- Documented `issue depend` command in `README.md` and updated `docs/prd/completed/native-dependency-links-PRD.md`.
- Moved YouTrack issues `YTCLI-36`, `YTCLI-37`, `YTCLI-38`, and Epic `YTCLI-35` to "Fixed" status.
- Note for the next person: The native dependency links PRD is fully complete. All issues in the project are resolved. There are no remaining open issues.

